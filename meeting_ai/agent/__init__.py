"""LiveKit Agent runtime package owned by Meeting AI.

Imports intentionally remain light so ``python -m meeting_ai.agent.worker``
does not import and execute the worker module twice during module resolution.
"""
